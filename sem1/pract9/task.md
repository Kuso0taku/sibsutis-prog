# Task  
Create the files `matrixfunc.h` and `matrixfunc.c`,  
where you will implement the functions described below for working with  
a two-dimensional array, hereinafter referred to as a matrix.  
In implementing the functions, use **pointers** and **pointer arithmetic**.  
**Pointers** must be used in **all loops**.  
The main file `main.c` contains the function `int main` and includes  
the header file `matrixfunc.h`.  
```
#define COL 10
#define ROW 10
```  
### Menu  
<ol type="a">
    <li>func #1 (<em>replace with input type name</em>) (enter matrix from file <)</li>
    <li>array representation (matrix of values) (specify indeces or numbers)</li>
    <li>func #2 (<em>replace with function name</em>)</li>
    <li>func #3 (<em>replace with function name</em>)</li>
    <li>exit</li>
</ol>

### Var 7  
- [x] **1** - left-to-right row-based input  
- [x] **16** - determining two-dimensional indices elements whose values are greater than the given k; in columns m through l  
- [x] **23** - determine the sum of all elements that are multiple of k  
