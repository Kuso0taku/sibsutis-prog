#include <wchar.h>

int cnt_chs(wchar_t *text); // counts how many every character
void ins_exclm_mark(wchar_t *text); // inserts exclemation mark in the on of each line
int cnt_dash_wrds(wchar_t *text); // finds and counts words with dashes
void ins_c_after_vwl(wchar_t *text); // inserts 'с' after vowel and repeats vowel
